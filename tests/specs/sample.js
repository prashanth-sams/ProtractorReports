describe('Test 2', function() {

	it('test 2', function() {
		browser.get('http://www.angularjs.org');

    $(".search-query").sendKeys("Prashanth Sams");

    // this.filterTeamDropdown = $("filter-item-edit .dropdown button");
    // this.teams = this.filterTeamDropdown.$$(this.filterTeamDropdown.locator().value + " + ul li.dropdown-list-item");

		// element.all(by.css("li.dropdown > a")).get(0).getText().then(function(value) {
    // 		console.log(value);
		// });
		// expect(protractor.getInstance().isElementPresent(element(by.css(".hero-unit")).find())).toBe(true);
		// element(by.css(".hero-unit")).isElementPresent(by.css("h1")).then(function(value) {
		// 		console.log(value);
		// });

    // console.log(element(by.css(".hero-unit")).locator().using);
    // console.log(element(by.css(".hero-unit")).locator().value);

		// element(by.css(".hero-unit")).element(by.css("h1")).isPresent().then(function(value) {
		// 		console.log(element(by.css(".hero-unit")).locator());
		// });

		// expect(element(by.css(".hero-unit")).isPresent()).toBe(true);

		// element.all(by.css("li.dropdown > a")).get(0).getText().then(function(value) {
		// 		console.log(value);
		// 		// action
		// });


		// element(by.css(".quick-links li a")).click().locator();
		//
		//
		// element.all(by.css('.selector')).then(function(elements) {


	// elements is an array of ElementFinders.
	  // });

		// element(by.css(".quick-links")).getWebElement().findElement(by.css("li a")).click();





// findElement
				// browser.findElement(by.css(".quick-links li a")).click();
				// browser.driver.findElement(by.css(".quick-links li a")).click();

// execute script
				// var elm = element(by.css(".quick-links li a"));
				// browser.executeScript("arguments[0].click()", elm.getWebElement());
















		// element.all(by.css(".quick-links li")).all(by.css("iframe")).count().then(function(value) {
		// 		console.log(value);
		// });


      // $("li.dropdown").click();
    // element(by.model('todoList.todoText')).sendKeys('selenium essentials');
		browser.sleep(5000);
		// element(by.css('[value="add"]')).click();

	});
});
